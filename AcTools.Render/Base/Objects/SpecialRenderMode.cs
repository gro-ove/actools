namespace AcTools.Render.Base.Objects {
    public enum SpecialRenderMode {
        Deferred,

        Reflection,
        Shadow,

        TransparentMask,
        TransparentDeferred,
        TransparentDepth,
        Transparent
    }
}