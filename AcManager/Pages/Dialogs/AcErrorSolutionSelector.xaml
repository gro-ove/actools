<mui:ModernDialog x:Class="AcManager.Pages.Dialogs.AcErrorSolutionSelector" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mui="http://firstfloorsoftware.com/ModernUI" mc:Ignorable="d" d:DesignHeight="300"
        d:DesignWidth="300">
    <Grid>
        <Grid Name="MessageModeControl" Visibility="Collapsed">
            <mui:BbCodeBlock Name="MessageModeBbCodeBlock" BbCode="None of solutions are available, sorry." />
        </Grid>
        <StackPanel Name="SelectModeControl" Visibility="Collapsed" Orientation="Vertical">
            <TextBlock Style="{StaticResource Heading2}" Text="Available solutions:" />
            <ListBox Name="SelectModeListBox" ItemsSource="{Binding Solver.Solutions}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="true"
                    VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" HorizontalAlignment="Stretch" ToolTip="{Binding Description}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource RadioListBoxItem}">
                        <Setter Property="Margin" Value="10 2 2 2" />
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </StackPanel>
    </Grid>
</mui:ModernDialog>
