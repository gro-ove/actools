<mui:ModernDialog x:Class="AcManager.Pages.Dialogs.FindInformationDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mui="http://firstfloorsoftware.com/ModernUI"
        xmlns:dialogs="clr-namespace:AcManager.Pages.Dialogs" xmlns:modernui="clr-namespace:FirstFloor.ModernUI;assembly=FirstFloor.ModernUI"
        xmlns:userControls="clr-namespace:AcManager.Controls.UserControls;assembly=AcManager.Controls" mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300"
        Title="{Binding SelectedObject.DisplayName, Mode=OneTime}" MaxWidth="1200" MaxHeight="900"
        d:DataContext="{d:DesignInstance dialogs:FindInformationDialog+FindInformationViewModel}">
    <mui:ModernDialog.ButtonsRowContent>
        <Button Command="{Binding SaveCommand}" Content="{Binding SaveLabel}" />
    </mui:ModernDialog.ButtonsRowContent>
    <StackPanel Margin="0 0 0 -36">
        <userControls:WebBrowserBlock Name="WebBrowser" StartPage="{Binding StartPage}" Navigated="WebBrowser_OnNavigated" Width="1080" Height="640" />
        <TextBlock FontStyle="Italic" Margin="8">Select year or description and press OK; hold Ctrl to extend description instead of replacing it.</TextBlock>
    </StackPanel>
</mui:ModernDialog>
