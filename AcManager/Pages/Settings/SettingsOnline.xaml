<UserControl x:Class="AcManager.Pages.Settings.SettingsOnline" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:networkInformation="clr-namespace:System.Net.NetworkInformation;assembly=System" xmlns:settings="clr-namespace:AcManager.Pages.Settings"
        xmlns:mui="http://firstfloorsoftware.com/ModernUI"
        xmlns:attached="clr-namespace:FirstFloor.ModernUI.Windows.Attached;assembly=FirstFloor.ModernUI"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300" FocusVisualStyle="{x:Null}"
        d:DataContext="{d:DesignInstance settings:SettingsOnline+SettingsOnlineViewModel}">
    <UserControl.Resources>
        <mui:SharedResourceDictionary Source="/AcManager.Controls;component/Assets/SettingsSpecific.xaml" />
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel Style="{StaticResource SettingsPanel}">
            <CheckBox IsChecked="{Binding Holder.RememberPasswords}">
                <Label>Remember passwords</Label>
            </CheckBox>

            <DockPanel>
                <Label>Server:</Label>
                <ComboBox ItemsSource="{Binding Holder.OnlineServers}" SelectedItem="{Binding Holder.OnlineServer}" DisplayMemberPath="DisplayName" />
            </DockPanel>

            <CheckBox IsChecked="{Binding Holder.UseFastServer}" attached:LimitedService.Limited="True">
                <Label>Use Fast Caching Server (experimental)</Label>
            </CheckBox>

            <TextBlock Style="{StaticResource SettingsPanel.Heading2}">Adding by IP:</TextBlock>

            <DockPanel>
                <Label>Scan ports:</Label>
                <TextBox Text="{Binding Holder.PortsEnumeration}" attached:WatermarkService.Watermark="?"
                        attached:FocusAdvancement.AdvancesByEnterKey="True">
                    <TextBox.ToolTip>Some enumeration like “9000-10000”</TextBox.ToolTip>
                </TextBox>
            </DockPanel>

            <TextBlock Style="{StaticResource SettingsPanel.Heading2}">LAN:</TextBlock>

            <!--<CheckBox IsChecked="{Binding Holder.AlternativeScanningMethod}" Margin="0 0 0 8" IsEnabled="False">
                <Label>Alternative scanning method</Label>
            </CheckBox>-->

            <DockPanel>
                <Label>Scan ports:</Label>
                <TextBox Text="{Binding Holder.LanPortsEnumeration}"
                         attached:WatermarkService.Watermark="?" attached:FocusAdvancement.AdvancesByEnterKey="True">
                    <TextBox.ToolTip>Some enumeration like “9600,9602,9700-9750”</TextBox.ToolTip>
                </TextBox>
            </DockPanel>

            <DockPanel>
                <Label>Scan using connections:</Label>
                <ListBox Name="IgnoredInterfacesListBox" ItemsSource="{Binding NetworkInterfaces}" SelectionMode="Multiple"
                        SelectionChanged="IgnoredInterfacesListBox_OnSelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="networkInformation:NetworkInterface">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" />
                                <TextBlock Text="{Binding Description}" Style="{StaticResource Small}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
