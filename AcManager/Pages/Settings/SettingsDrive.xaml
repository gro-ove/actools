<UserControl x:Class="AcManager.Pages.Settings.SettingsDrive" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:watermark="clr-namespace:FirstFloor.ModernUI.Windows.Controls.Watermark;assembly=FirstFloor.ModernUI"
        xmlns:networkInformation="clr-namespace:System.Net.NetworkInformation;assembly=System" xmlns:settings="clr-namespace:AcManager.Pages.Settings"
        xmlns:mui="http://firstfloorsoftware.com/ModernUI" xmlns:helpers="clr-namespace:AcManager.Tools.Helpers;assembly=AcManager.Tools" mc:Ignorable="d"
        d:DesignHeight="300" d:DesignWidth="300" FocusVisualStyle="{x:Null}" d:DataContext="{d:DesignInstance settings:SettingsDrive+SettingsDriveViewModel}">
    <UserControl.Resources>
        <mui:SharedResourceDictionary Source="/AcManager.Controls;component/Assets/SettingsSpecific.xaml" />
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel Style="{StaticResource SettingsPanel}">
            <DockPanel>
                <Label>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>
                            <Run>Game starter</Run><Hyperlink Typography.Variants="Superscript" FontSize="11" BaselineAlignment="Superscript"
                                    Command="{Binding NavigateCommand}" CommandParameter="/Pages/About/ImportantTipsPage.xaml?Key=starters">[?]</Hyperlink><Run>:</Run>
                        </TextBlock>
                    </StackPanel>
                </Label>
                <ComboBox ItemsSource="{Binding Holder.StarterTypes}" SelectedItem="{Binding Holder.SelectedStarterType}" DisplayMemberPath="DisplayName">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                            <Setter Property="IsEnabled" Value="{Binding IsAvailable}" />
                            <Setter Property="ToolTip" Value="{Binding Description}" />
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
            </DockPanel>

            <CheckBox IsChecked="{Binding Holder.Use32BitVersion}">
                <Label>Use 32-bit version</Label>
            </CheckBox>

            <CheckBox IsChecked="{Binding Holder.ImmediateStart}">
                <Label>Start race immediately</Label>
            </CheckBox>

            <CheckBox IsChecked="{Binding Holder.SkipPracticeResults}">
                <Label>Don't show results of Practice session</Label>
            </CheckBox>

            <TextBlock Style="{StaticResource SettingsPanel.Heading2}">Player's name:</TextBlock>

            <DockPanel>
                <Label>Name:</Label>
                <TextBox Text="{Binding Holder.PlayerName}" watermark:WatermarkService.Watermark="?" />
            </DockPanel>

            <DockPanel>
                <Label>Nationality:</Label>
                <TextBox Text="{Binding Holder.PlayerNationality}" watermark:WatermarkService.Watermark="?" />
            </DockPanel>

            <DockPanel>
                <Label>Online name:</Label>
                <CheckBox DockPanel.Dock="Left" IsChecked="{Binding Holder.DifferentPlayerNameOnline}" ToolTip="Use different name online"
                        VerticalAlignment="Center" />
                <TextBox Text="{Binding Holder.PlayerNameOnline}" watermark:WatermarkService.Watermark="?"
                        IsEnabled="{Binding Holder.DifferentPlayerNameOnline}" />
            </DockPanel>

            <TextBlock Style="{StaticResource SettingsPanel.Heading2}">Kunos career:</TextBlock>

            <CheckBox IsChecked="{Binding Holder.KunosCareerUserAiLevel}">
                <Label>Allow to change AI level</Label>
            </CheckBox>

            <CheckBox IsChecked="{Binding Holder.KunosCareerUserSkin}">
                <Label>Allow to change car's skin</Label>
            </CheckBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
