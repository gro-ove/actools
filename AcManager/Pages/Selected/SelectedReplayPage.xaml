<selected:SelectedAcObjectPage x:Class="AcManager.Pages.Selected.SelectedReplayPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ac="clr-namespace:AcManager.Controls;assembly=AcManager.Controls"
        xmlns:mui="http://firstfloorsoftware.com/ModernUI" xmlns:selected="clr-namespace:AcManager.Pages.Selected"
        xmlns:watermark="clr-namespace:FirstFloor.ModernUI.Windows.Controls.Watermark;assembly=FirstFloor.ModernUI" mc:Ignorable="d" d:DesignHeight="300"
        d:DesignWidth="300" mui:InputBindingBehavior.PropagateInputBindingsToWindow="True" FocusVisualStyle="{x:Null}">
    <ac:AcObjectBase AcObject="{Binding SelectedObject}" ShowIcon="False">
        <ac:AcObjectBase.ToolBars>
            <ToolBar ToolBarTray.IsLocked="True" Band="0" BandIndex="0">
                <Button Content="View in Explorer" ToolTip="View replay file in explorer (Ctrl+F)" Command="{Binding SelectedObject.ViewInExplorerCommand}" />
                <Button Content="Reload" ToolTip="Reload (Ctrl+R)" Command="{Binding SelectedObject.ReloadCommand}" CommandParameter="full" />
                <Separator />
                <Button Content="Play" Command="{Binding PlayCommand}" ToolTip="Run replay (Ctrl+G)" Style="{StaticResource Go.ToolBarButton}" />
            </ToolBar>
            <ToolBar Band="1" BandIndex="1">
                <Button Content="Disable" Command="{Binding SelectedObject.ToggleCommand}" ToolTip="Disable (Ctrl+D)"
                        Visibility="{Binding SelectedObject.Enabled, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button Content="Enable" Command="{Binding SelectedObject.ToggleCommand}" ToolTip="Enable (Ctrl+D)"
                        Visibility="{Binding SelectedObject.Enabled, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=inverse}" />
                <Button Content="Save" Command="{Binding SelectedObject.SaveCommand}" ToolTip="Rename file according to the new name (Ctrl+S)" />
                <Button Content="Delete" Command="{Binding SelectedObject.DeleteCommand}" ToolTip="Remove object to Recycle Bin (Ctrl+Delete)" />
            </ToolBar>
        </ac:AcObjectBase.ToolBars>

        <StackPanel>
            <StackPanel DataContext="{Binding SelectedObject}"
                    Visibility="{Binding ParsedSuccessfully, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid Margin="0 8 8 8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Track:" Grid.Column="0" Grid.Row="0" Style="{StaticResource LabelWithPadding}" />
                    <TextBlock Text="Track Configuration:" Grid.Column="0" Grid.Row="1" Style="{StaticResource LabelWithPadding}" />

                    <TextBlock Text="{Binding TrackId}" Grid.Column="1" Grid.Row="0" />
                    <TextBlock Text="{Binding TrackConfiguration}" Grid.Column="1" Grid.Row="1" />

                    <TextBlock Text="Weather:" Grid.Column="2" Grid.Row="0" Style="{StaticResource LabelWithPadding}" />
                    <TextBlock Text="Date:" Grid.Column="2" Grid.Row="1" Style="{StaticResource LabelWithPadding}" />

                    <TextBlock Text="{Binding WeatherId}" Grid.Column="3" Grid.Row="0" />
                    <TextBlock Text="{Binding Date}" Grid.Column="3" Grid.Row="1" />
                </Grid>
            </StackPanel>
        </StackPanel>
    </ac:AcObjectBase>
</selected:SelectedAcObjectPage>
