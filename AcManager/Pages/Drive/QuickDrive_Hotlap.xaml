<UserControl x:Class="AcManager.Pages.Drive.QuickDrive_Hotlap" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mui="http://firstfloorsoftware.com/ModernUI"
        xmlns:system="clr-namespace:System;assembly=mscorlib" xmlns:drive="clr-namespace:AcManager.Pages.Drive" mc:Ignorable="d" d:DesignHeight="300"
        d:DesignWidth="300" FocusVisualStyle="{x:Null}">

    <UserControl.Resources>
        <Thickness x:Key="ElementMargin">0 0 10 20</Thickness>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical" Margin="{StaticResource ElementMargin}" Grid.Row="0" Grid.Column="0">
                    <CheckBox Margin="10 0 0 0" IsChecked="{Binding Penalties}">
                        <Label Content="_Penalties" />
                    </CheckBox>
                </StackPanel>

                <StackPanel Orientation="Vertical" Margin="{StaticResource ElementMargin}" Grid.Row="0" Grid.Column="1">
                    <CheckBox Margin="10 0 0 0" IsChecked="{Binding GhostCar}">
                        <Label Content="_Ghost car" />
                    </CheckBox>
                </StackPanel>

                <StackPanel Orientation="Vertical" Margin="{StaticResource ElementMargin}" Grid.Row="1" Grid.Column="1">
                    <TextBlock Style="{StaticResource LabelWithPadding}" Margin="0 0 0 5"
                            Text="{Binding GhostCarAdvantage, StringFormat='Ghost car advantage: \{0:F2\} sec'}" />
                    <Slider Minimum="0" Maximum="5" Value="{Binding GhostCarAdvantage}" />
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
